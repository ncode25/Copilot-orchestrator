# Copilot-orchestrator
The Orchestrator is like a project manager — it never does the work itself. It asks the Planner to design the approach, then assigns the Coder to build it. It checks the work is done, handles mistakes, and gives you a summary at the end.
